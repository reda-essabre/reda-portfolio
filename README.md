# Reda Portfolio

Current portfolio for `reda.cx`.

- `v2/` contains the active Next.js, TypeScript, and Tailwind source.
- The root `index.html`, `_next/`, and related static files are the exported build deployed to `reda.cx`.

Build from source:

```bash
cd v2
npm run build
```
